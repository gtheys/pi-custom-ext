/**
 * Worktree Extension — placeholder scaffold.
 *
 * Will provide worktree tools (create/list/remove) wrapping Herdr
 * worktree workspaces. Tool registration comes in Phase 2.
 */

import type { ExtensionAPI } from '@earendil-works/pi-coding-agent'

export default function (_pi: ExtensionAPI) {
  // AIDEV-NOTE: scaffold only — tool registration lands in Phase 2.
}
